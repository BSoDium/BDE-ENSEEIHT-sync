const jsonSettings = {
  pollingInterval: 30, // minutes (must be 1, 5, 10, 15, or 30) - defines how frequently the script will run
  userName: "l3alr0g", // change this to your name / nickname
  maxDescriptionLength: 180 // maximum lenght (in chars) of the event's description
}